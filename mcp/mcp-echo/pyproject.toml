[project]
name = "mcp-echo"
version = "0.1.0"
description = "回显测试 MCP：用于验证 MCP 连接是否正常"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0,<3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["mcp_echo.py"]
